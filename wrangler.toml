name = "playfairs"
main = "./.open-next/worker.js"
compatibility_date = "2024-11-01"
compatibility_flags = ["nodejs_compat"]

[build]
command = "npm run build && npm run build:worker && wrangler deploy --env=\"\""

[vars]
ENVIRONMENT = "production"
